/**
 * Numeric digit strings matching the scope flags in Injector
 *
 * @enum {String}
 * @readonly
 * @package
 */
const Scope = {
  SINGLETON: "1",
  PROTOTYPE: "2",
  APPLICATION: "3"
};

module.exports = Scope;